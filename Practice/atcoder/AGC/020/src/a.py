# 先に相手の隣に立ったら負け確定
# 一方が距離を縮め、一方が逆方向にいく -> 距離は縮まらない(+-0)
# 両方とも縮める/外側にいく -> 距離は2減る/増える
# 先攻と後攻の間が1 -> 先攻が先に横に立つので負け
# 2 -> 後攻, 3 -> 先攻, .....
n, a, b = map(int, input().split())
dif = max(a, b) - min(a, b)
print("Borys" if dif % 2 == 1 else "Alice")
